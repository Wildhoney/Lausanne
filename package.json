{
  "name": "lausanne",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "build": "nx run-many --target=build",
    "start": "nx run-many --target=start",
    "format": "nx run-many --target=format"
  },
  "devDependencies": {
    "nx": "15.7.2",
    "prettier": "2.8.4",
    "typescript": "4.9.5"
  }
}
